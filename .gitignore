# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Node
node_modules/
npm-debug.log*

# Build artifacts
*.zip
dist/
safari-xcode/
safari-build/

# SVG source files for PNG icons (icon-base and icon-white are used at runtime)
icons/icon48.svg
icons/icon128.svg

# Local settings
.claude/

# Local-only docs holding credentials/secrets (referenced from CLAUDE.md)
*.local.md

# Git worktrees
.worktrees/

#
*.zip

CLADE.md

# Post-quantum key files. The guide tells users to generate keys.json inside a clone
# of this repo, so make it impossible to commit one by accident — it holds secret keys.
keys.json
*.keyfile.json
